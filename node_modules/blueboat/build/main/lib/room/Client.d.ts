import { Server } from 'socket.io';
declare class Client {
    id: string;
    sessionId: string;
    origin: string;
    ip: string;
    send: (key: string, data?: any) => void;
    removeFromRoom: () => void;
    constructor(roomId: string, id: string, sessionId: string, origin: string, ip: string, io: Server, remove: (clientSessionId: string, intentional: boolean) => void);
}
export default Client;
