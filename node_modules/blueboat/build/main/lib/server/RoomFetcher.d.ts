import { RoomSnapshot } from '../../types/RoomSnapshot';
import Storage from '../storage/Storage';
interface RoomFetcherOptions {
    storage: Storage;
}
/**
 * Can help find a list of currently available Rooms and their snapshots
 */
declare class RoomFetcher {
    storage: Storage;
    constructor(options: RoomFetcherOptions);
    getListOfRooms: () => Promise<string[]>;
    getListOfRoomsWithData: () => Promise<RoomSnapshot[]>;
    findRoomById: (roomId: string) => Promise<any>;
    setRoomMetadata: (roomId: string, newMetadata: any) => Promise<any>;
    addRoom: (room: RoomSnapshot) => Promise<void>;
    removeRoom: (roomId: string) => Promise<void>;
}
export default RoomFetcher;
