import Storage from '../storage/Storage';
interface CustomGameValueOptions {
    storage: Storage;
}
/**
 * Used for global game values in which you can change in the admin panel
 */
declare class CustomGameValues {
    storage: Storage;
    constructor(options: CustomGameValueOptions);
    setGlobalGameValuesObject: (newGameValues: any) => Promise<void>;
    getGameValues: () => Promise<any>;
    getGameValue: <T>(key: string, defaultValue?: T) => Promise<T>;
    setGameValue: (key: string, value?: any) => Promise<void>;
    resetGameValues: () => Promise<void>;
}
export default CustomGameValues;
