import { Server } from 'socket.io';
import AvaiableRoomType from '../../../types/AvailableRoomType';
import SimpleClient from '../../../types/SimpleClient';
import PubSub from '../../pubsub/PubSub';
import Room from '../../room/Room';
import Storage from '../../storage/Storage';
import CustomGameValues from '../CustomGameValues';
import RoomFetcher from '../RoomFetcher';
declare const CreateNewRoom: (client: SimpleClient, io: Server, roomFetcher: RoomFetcher, gameValues: CustomGameValues, pubsub: PubSub, storage: Storage, availableRooms: AvaiableRoomType[], onRoomDisposed: (roomId: string) => void, roomName: string, existingRoomIds: string[], creatorOptions: any, customRoomIdGenerator?: (roomName: string, roomOptions?: any, creatorOptions?: any) => string) => Promise<Room<any>>;
export default CreateNewRoom;
