export declare type OnFunction = (key: string, callback: (data: any) => any) => {
    unsubscribe: () => any;
};
export declare type Publish = (key: string, data: any) => any;
declare class PubSub {
    on: OnFunction;
    publish: Publish;
    constructor(on: OnFunction, publish: Publish);
}
export default PubSub;
