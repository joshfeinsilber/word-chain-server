import { Client, Room } from '../index';
import State from './State';
declare class ChatRoom extends Room<State> {
    onCreate(): Promise<void>;
    onJoin(client: Client): void;
    onMessage(client: Client, action: string, data?: any): void;
    beforeDispose(): Promise<void>;
    onLeave: (client: Client) => Promise<void>;
    private addMessage;
    private catchUp;
}
export default ChatRoom;
